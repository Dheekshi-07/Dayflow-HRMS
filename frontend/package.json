{
  "name": "hr-management-app",
  "version": "1.0.0",
  "description": "HRFlow — Professional HR Management Web Application",
  "scripts": {
    "dev": "npx -y live-server --port=3000 --no-browser",
    "start": "npx -y live-server --port=3000"
  }
}
